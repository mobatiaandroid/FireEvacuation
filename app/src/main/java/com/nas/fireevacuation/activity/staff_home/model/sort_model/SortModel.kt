package com.nas.fireevacuation.activity.staff_home.model.sort_model

import com.nas.fireevacuation.activity.staff_home.model.students_model.Lists

data class SortModel(
    var index: String,
    var list: ArrayList<Lists>
)
